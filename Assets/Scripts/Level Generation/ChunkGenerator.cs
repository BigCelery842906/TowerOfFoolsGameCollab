using NUnit.Framework;
using System.Collections.Generic;
using UnityEngine;

public class ChunkGenerator : MonoBehaviour
{

    [SerializeField]
    public List<GameObject> m_wallPool;
    public List<GameObject> m_floorPool;
    public List<GameObject> m_backgroundPool;


    private void Awake()
    {
        
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void SpawnStartingRoom(float w, float h)
    {

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
